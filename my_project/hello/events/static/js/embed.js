// static/js/embed.js
function initEmbeddedCalendar(config) {
    const container = document.getElementById(config.container);
    
    // Kalender initialisieren mit config-Einstellungen
    // AJAX-Aufruf für Events mit den Filterparametern
}